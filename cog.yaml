image: "r8.im/alijazup/quran-asr"
build:
  gpu: true
  cuda: "11.8"
  system_packages:
    - "ffmpeg"
  python_version: "3.10"
  python_packages:
    - "faster-whisper"
    - "soundfile"
    - "numpy"
  run:
    - "python3 -c \"from faster_whisper import WhisperModel; WhisperModel('large-v3-turbo', download_root='/src/weights')\""
predict: "predict.py:Predictor"
